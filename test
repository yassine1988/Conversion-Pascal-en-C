PROGRAM prog;

{ function f }
FUNCTION f (x,y:INTEGER;test:STRING) :INTEGER;
VAR i,ii:INTEGER;toto:STRING;
VAR test:STRING;
CONST truc:INTEGER;
BEGIN
    i := 0;
    f := 0;
    f:= 3+(5*9.3)-(-f(3));
    te := 'tttt';
    tee := f(5);
    bool := false;
    write('toto');
    WHILE i < x DO
    BEGIN
	IF i MOD 2 = 0 THEN
	      f := f + i;
    END;
END;

{ main program }
VAR x:INTEGER;
BEGIN
    READLN(x);
    WRITELN(f(x));
END.
