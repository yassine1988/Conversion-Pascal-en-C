PROGRAM prog;

{ function f }
FUNCTION f (x,y:INTEGER;test:STRING) :INTEGER;
VAR i,ii:INTEGER;toto:STRING;names:ARRAY[1..10] OF INTEGER;
CONST truc:INTEGER;
BEGIN
    i := 0;
    f := 0;
    f:= 3+(5*9.3)-(-f(3));
    te := 'tttt';
    tee := f(5);
    bool := false;
    names[1] := 3;
    write('toto');
    
    for i:= 1 to 10 do
    begin
		writeln('toto');
		readln(marks[i]);
    end;
    
    for i:=1 to 10 do
		writeln(toto);
    
    WHILE i < x DO
    BEGIN
	IF i MOD 2 = 0 THEN
	      f := f + i;
    END;
END;

{ main program }
VAR x:INTEGER;
BEGIN
    READLN(x);
    WRITELN(f(x));
END.
